import React from 'react';
import './Headline.scss';

const headline = (props) => (
    <div className="headline">
        <h1 className="headline-text">Toronto Waste Lookup</h1>
    </div>
);

export default headline;

